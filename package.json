{
  "name": "grunt-cache-cdn",
  "description": "Download cdn libraries for local use (e.g. unit tests). Define your cdn libs in one place and write the references into your html.",
  "version": "0.1.0",
  "homepage": "https://github.com/cmplank/grunt-cache-cdn",
  "author": {
    "name": "Colin Plank"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/cmplank/grunt-cache-cdn.git"
  },
  "bugs": {
    "url": "https://github.com/cmplank/grunt-cache-cdn/issues"
  },
  "license": "MIT",
  "engines": {
    "node": ">= 7.0.0"
  },
  "scripts": {
    "test": "grunt test"
  },
  "devDependencies": {
    "grunt-contrib-jshint": "^0.9.2",
    "grunt-contrib-clean": "^0.5.0",
    "grunt-contrib-nodeunit": "^0.3.3",
    "grunt": "~0.4.5"
  },
  "keywords": [
    "gruntplugin"
  ],
  "dependencies": {
    "cache-cdn": "^1.0.0"
  }
}
